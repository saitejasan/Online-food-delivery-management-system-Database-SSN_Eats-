1) locate to the package.json file, cd to that location and type npm install to install necessary packages.
2) install nodemon with npm install -g nodemon.
3) nodemon app.js to start the website that is hosted at http://localhost:3000/
4) login with username : saiteja, password : 123
5) navigate through the navbar to different routes each handling tables in the database.
6) as an admin to manage the details, creating, searching, deleting and querying is provided.

Authors: 	Sai Teja Sankarneni
